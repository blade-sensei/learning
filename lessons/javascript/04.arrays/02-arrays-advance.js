const simple = [0, 1, 2];

// javascript provides you some methods that can be used without arrays
/* -  length
-  toString()
-  pop()
-  push()
-  shift()
-  unshift()
-  join()
-  delete()
-  concat()
-  flat()
-  splice()
-  slice()
-  map()
- slice()
- filter()
- reduce()
 */
/**
 * most commun methods
 * map, slice, filter, every, some, reduce
 * this function are used choose with elements to get from the list
 */

// slice means CUT before the INDEX number
console.log(simple.slice(1));
console.log(simple.slice(1));

/**
 * ref; https://www.w3schools.com/js/js_array_methods.asp
 * https://javascript.info/array-methods
 */
